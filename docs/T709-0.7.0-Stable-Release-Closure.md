# Icod.DCurses 0.7.0 — T709 Stable Release Closure

**Release line:** `0.7.0`  
**Release-candidate checkpoint:** `0.7.0-rc.1`  
**Stable-source version:** `0.7.0`  
**Assembly version:** `0.7.0.0`  
**Stable predecessor:** `0.6.0`  
**Dependencies:** `Icod.Terminal 1.0.0`; `Icod.TermInfo 1.10.0`  
**Status:** Stable-source merge gate active

## Purpose

T709 is release closure only. The 0.7 refresh/output optimization contract is frozen; no new feature family, optimization strategy, or public API may enter during this tranche.

The accepted public delta is frozen in `docs/Public-API-Baseline-0.7.md`. The implementation and regret decisions are recorded by T701 through T708, culminating in `docs/T708-Benchmark-API-Package-and-Regret-Gate.md`.

## Release-candidate gate

The exact `0.7.0-rc.1` head `637cd6151251f9c247045ee671c12236208d5988` passed:

- Windows Staging restore/build/test;
- Linux Staging restore/build/test;
- macOS Staging restore/build/test;
- canonical Staging package validation;
- fresh package-only consumption of the generated artifact;
- warning level 4 / warnings-as-errors for library and tests;
- the frozen 0.7 public API reflection guard;
- deterministic T708 refresh/output regret workloads;
- failure/invalidation recovery tests for cursor, erase, character-shift, line-shift, scroll-region, and rendition paths.

No runtime behavior or public API changed during stable promotion.

## Stable promotion

Stable-source promotion changes release-state metadata only:

```text
Version         0.7.0
PackageVersion  0.7.0
AssemblyVersion 0.7.0.0
```

The stable package release notes describe the already-validated 0.7 contract:

- opt-in Terminal-owned synchronized-output framing;
- cost-aware cursor-motion and erase selection;
- exact character-shift optimization;
- exact line-shift/scroll optimization with failure-safe temporary-region restoration;
- differential retained-rendition transitions;
- deterministic byte/write optimization gates;
- one public addition: `CursesSessionOptions.UseSynchronizedOutput`, default `false`.

All cost models, selectors, plans, retained physical state, and measurement infrastructure remain internal.

## Stable-source merge gate

The exact stable-source head must pass the same PR matrix and package/fresh-consumer validation after all T709 documentation and metadata are synchronized. After that result, the branch must remain untouched except for PR metadata.

Only that validated stable source head is eligible to merge to `main`.

This document does **not** authorize or perform that merge.

## Post-merge publication gate

After a later explicit merge:

1. identify the exact merge commit on `main`;
2. require the full Release x64/ARM64 matrix and Release package validation to pass on that exact commit;
3. create `v0.7.0` only on that exact green `main` commit;
4. verify NuGet.org and GitHub Packages publication;
5. verify `.nupkg`, `.snupkg`, checksum, and GitHub Release assets;
6. verify the published package reports `0.7.0` and the dependency baseline remains `Icod.Terminal 1.0.0` plus `Icod.TermInfo 1.10.0`.

A tag must never be used to bypass a failing post-merge Release gate.
