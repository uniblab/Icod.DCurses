# T1003 — 1.0 Documentation, Package, and Release Audit

**Release line:** `1.0.0`  
**Candidate:** `1.0.0-rc.1`  
**Status:** audit complete; no runtime/public-contract correction required

## Audit result

The final 1.0 documentation/package/release review found no defect requiring a production-code or public-API change.

The candidate remains the exact 0.9 public and semantic contract under the new stable major-version identity.

## Documentation corrections

The root README was corrected for the post-0.9-merge state:

- `1.0.0-rc.1` is identified as the active stable-release candidate;
- the exact 0.9 contract fingerprint carried into 1.0 is prominent;
- stale wording saying 0.9 was still awaiting merge was removed;
- merged/validated 0.9 source is distinguished from published package state;
- `0.8.0` remains the installation example because `v0.9.0` was not a published GitHub release during this audit;
- stable 1.0 installation is not advertised before `v1.0.0` publication;
- the 1.0 compatibility, validation, release-documentation, Authors, Copyright, and License sections are synchronized with the candidate.

A dedicated `docs/1.0-Stable-Compatibility-and-Migration-Guide.md` was added. It states that the frozen 0.9-to-1.0 transition has no planned source migration and records the stable assembly/dependency/geometry/Unicode/lifetime/failure contracts.

The authoritative `Icod.DCurses-1.0.0-Development-Roadmap.md` now describes T1001–T1005 as the only active release-closure sequence and records the exact green post-0.9 `Release` baseline.

Historical 0.1–0.9 tranche records remain historical and were not mass-rewritten to current versions.

## API and compatibility audit

The candidate retains:

```text
sha256:            274b87ec28a253e4891f7f72dea847eaf7d57f45e7b6dd2ae4b464e783046639
exported types:    43
contract lines:   309
```

`docs/Public-API-Fingerprint-1.0.json` is intentionally identical in contract content to `docs/Public-API-Fingerprint-0.9.json`.

The candidate test assembly carries both baselines, and `PublicStableApiBaselineTests` rejects divergence in SHA, exported-type count, contract-line count, or exported-type inventory.

The established compiled fingerprint test and semantic/hardening/dependency-boundary suites continue to guard the actual assembly contract and behavior.

## Dependency audit

The direct runtime dependencies remain exactly:

```text
Icod.Terminal 1.4.0
Icod.TermInfo 1.10.0
```

The package verifier continues to require those exact dependency groups for `net8.0`, `net9.0`, and `net10.0`.

The GitHub Release workflow continues deriving displayed dependency versions from the project `PackageReference` values rather than duplicating version literals.

## Package audit

The first 1.0 RC foundation package passed package/fresh-consumer validation with:

```text
Version         1.0.0-rc.1
PackageVersion  1.0.0-rc.1
AssemblyVersion 1.0.0.0
```

Validation covers package metadata, LGPL-3.0-or-later license metadata and required acceptance, README/icon/license payloads, repository metadata, all three TFM assemblies/XML documentation files, portable symbols, exact dependencies, and a fresh package-only consumer.

## Authors, copyright, license, and naming audit

The README retains historical attribution to `termcap`, `terminfo`, `pcurses`, `libtinfo`, and `ncurses`, followed by the managed implementation credit to Timothy J. Bruce.

Current prose writes `ncurses` as `` `ncurses` ``.

The copyright remains:

```text
Copyright (c) 2026 Timothy J. Bruce
```

The package and README remain LGPL-3.0-or-later with required license acceptance.

## Workflow audit

The repository retains:

- PR `Staging` validation across Windows x64/ARM64, Linux x64/ARM64, macOS x64/ARM64, plus package validation;
- `main` `Release` validation across the same six runtime architectures plus package validation;
- tag/version/main-ancestry checks before release publication;
- NuGet.org and GitHub Packages publication from the validated package artifact;
- symbol packages and generated SHA-256 release checksums;
- GitHub Release dependency wording derived from project metadata.

The merged 0.9 baseline `2acf166aed9c56c330025ca923c1d7cae712f913` passed the complete post-merge `Release` workflow before 1.0 development began.

## Acceptance audit

The 1.0 candidate retains all established representative acceptance families: editor, pager, Unicode, rich input, lifecycle, ownership, failure recovery, pad/viewport, large-screen, sparse/high-frequency/no-op refresh, and `top`/`slabtop`/`watch` sample builds.

The first 1.0 RC foundation head passed all six PR runtime architectures and package/fresh-consumer validation without a contract change.

## Conclusion

T1003 is complete. No unresolved mismatch remains between the `1.0.0-rc.1` candidate contract, package, release workflow, and current consumer/maintainer documentation.

T1004 may now freeze one exact documentation-synchronized RC head for the definitive seven-job gate.