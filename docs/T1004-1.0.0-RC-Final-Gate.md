# T1004 — 1.0.0 Release-Candidate Final Gate

**Release line:** `1.0.0`  
**Candidate:** `1.0.0-rc.1`  
**Assembly version:** `1.0.0.0`  
**Dependencies:** `Icod.Terminal 1.4.0`; `Icod.TermInfo 1.10.0`  
**Status:** definitive RC gate active

## Objective

T1004 proves one exact documentation-synchronized `1.0.0-rc.1` head suitable for unchanged promotion to stable `1.0.0`.

No feature, public API, enum, semantic, dependency, or ownership change is permitted in T1004.

## Frozen contract

```text
sha256:            274b87ec28a253e4891f7f72dea847eaf7d57f45e7b6dd2ae4b464e783046639
exported types:    43
contract lines:   309
```

The 1.0 machine baseline must remain identical to the 0.9 freeze, and the compiled assembly must continue to reproduce that fingerprint for every target framework.

## Required runtime matrix

The exact candidate must pass under PR Staging policy on:

- Windows x64;
- Windows ARM64;
- Linux x64;
- Linux ARM64;
- macOS x64;
- macOS ARM64.

The test matrix covers `net8.0`, `net9.0`, and `net10.0`.

## Package gate

The exact candidate must also pass generated-package validation including:

- package/assembly version identity;
- all three framework assemblies and XML documentation payloads;
- portable symbols and `.snupkg`;
- exact Terminal 1.4.0 / TermInfo 1.10.0 dependency groups;
- README/license/icon/repository metadata;
- fresh package-only consumer compilation and execution.

## Compatibility/acceptance gate

The complete existing suite remains required, including:

- actual compiled public API fingerprint;
- 0.9-to-1.0 baseline equality;
- dependency boundary;
- geometry/Unicode/cell semantic freeze;
- lifetime/concurrency/cancellation/disposal/failure hardening;
- editor/pager/refresh operation-selection regressions;
- rich input and lifecycle acceptance;
- pad/viewports and scale/high-frequency/no-op refresh;
- `top`, `slabtop`, and `watch` acceptance sample builds.

## T1003 prerequisite

The documentation/package/release audit is complete before this gate. README and the authoritative roadmap describe `1.0.0-rc.1` as the active candidate, retain the latest actually published package in installation guidance, and document the exact stable compatibility contract.

## Gate result

Only one exact head which satisfies all requirements above may be promoted to stable `1.0.0` in T1005.

The exact SHA and workflow run are recorded in PR metadata after the matrix closes successfully so recording the result does not move the tested branch head.