# T1004 — 1.0.0 Release-Candidate Final Gate

**Release line:** `1.0.0`  
**Candidate:** `1.0.0-rc.1`  
**Assembly version:** `1.0.0.0`  
**Dependencies:** `Icod.Terminal 1.4.0`; `Icod.TermInfo 1.10.0`  
**Status:** complete

## Objective

T1004 proves one exact documentation-synchronized `1.0.0-rc.1` head suitable for unchanged promotion to stable `1.0.0`.

No feature, public API, enum, semantic, dependency, or ownership change is introduced by T1004.

## Frozen contract

```text
sha256:            274b87ec28a253e4891f7f72dea847eaf7d57f45e7b6dd2ae4b464e783046639
exported types:    43
contract lines:   309
```

The 1.0 machine baseline remained identical to the 0.9 freeze, and the compiled assembly reproduced that fingerprint for every target framework.

## Exact accepted RC

```text
1968bae18610e69e56dc8f720bffb099cb58eb24
```

Workflow run `34371426709` (#389) completed successfully on that exact SHA.

## Runtime matrix result

Passed under PR Staging policy on:

- Windows x64;
- Windows ARM64;
- Linux x64;
- Linux ARM64;
- macOS x64;
- macOS ARM64.

The test matrix covered `net8.0`, `net9.0`, and `net10.0`.

## Package result

The exact candidate passed generated-package validation including:

- package/assembly version identity;
- all three framework assemblies and XML documentation payloads;
- portable symbols and `.snupkg`;
- exact Terminal 1.4.0 / TermInfo 1.10.0 dependency groups;
- README/license/icon/repository metadata;
- fresh package-only consumer compilation and execution.

## Compatibility/acceptance result

The complete existing suite passed, including:

- actual compiled public API fingerprint;
- 0.9-to-1.0 baseline equality;
- dependency boundary;
- geometry/Unicode/cell semantic freeze;
- lifetime/concurrency/cancellation/disposal/failure hardening;
- editor/pager/refresh operation-selection regressions;
- rich input and lifecycle acceptance;
- pad/viewports and scale/high-frequency/no-op refresh;
- `top`, `slabtop`, and `watch` acceptance sample builds.

## Gate decision

T1004 is accepted. Exact RC head `1968bae18610e69e56dc8f720bffb099cb58eb24` is the sole release-candidate basis for T1005 stable promotion.
