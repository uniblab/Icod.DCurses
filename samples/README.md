# Icod.DCurses samples

`Icod.DCurses.Sample` is the repository sample application.

During T01 it intentionally verifies only that the root library project is built
and loadable. Interactive session, input, window, and refresh demonstrations will
be added as the corresponding 0.1.0 tranches are implemented.
